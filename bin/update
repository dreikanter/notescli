#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

echo "Pulling latest changes..."
git checkout main
git pull --tags

echo "Building and installing..."
make install

echo "Installed: $(notes --version)"
